[tool.poetry]
name = "hexprobe"
version = "1.0.0"
description = "Multi-agent self-evolving repo auditing system"
authors = ["HexProbe Team"]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
